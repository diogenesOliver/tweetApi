package controllers

/* import (
	"net/http"
	"net/http/httptest"
	"testing"
)

func FindAllTweets_Test(t *testing.T) {
	rr := httptest.NewRecorder()
	FAKE_REQUEST := httptest.NewRequest("GET", "/tweets", nil)

	NewTweetController().FindAll(rr, FAKE_REQUEST)
	if http.StatusOK != rr.Code {
		t.Errorf(
			"Status code expected: %d, got %d",
			http.StatusOK,
			rr.Code,
		)
	}
} */
